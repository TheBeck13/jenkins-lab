pipeline {
    agent { 
      dockerfile true 
      label 'test'
    }

    stages {
        stage('Hello') {
            steps {
                sh 'node --version'
                sh 'git --version'
                sh 'curl --version'
            }
        }
    }
}
