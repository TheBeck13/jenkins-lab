pipeline {
    agent { 
      dockerfile true 
      label 'test'
    }

    stages {
        stage('Hello') {
            steps {
                sh 'git --version'
                sh 'curl --version'
            }
        }
    }
}
